[Unit]
Description=Kanata
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/kanata/kanata_linux_x64 --cfg /usr/bin/kanata/kanata.kbd
Restart=always

[Install]
WantedBy=multi-user.target   